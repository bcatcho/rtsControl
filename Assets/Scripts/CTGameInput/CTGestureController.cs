using UnityEngine;
using System.Collections;

/// <summary>
/// CT gesture controller.
/// 
/// Intercept input messages
/// determine if it is part of a current gesture or new
/// pass it off to the appropriate gesture interpreter/state machine(?) in a preallocated gesture FSM pool
/// </summary>
public class CTGestureController : MonoBehaviour
{

	
	// Update is called once per frame
	void Update()
	{
	
	}
}
